PORT=5000
NODE_ENV=development
DATABASE_URL="file:./dev.db"
# REDIS_URL=redis://localhost:6379 # Optional, falls back to memory cache if empty
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
